Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
python-dotenv>=1.0.0
pyodbc>=5.0.0
bcrypt>=4.1.0
opencv-python-headless>=4.9.0
pytesseract>=0.3.10
Pillow>=10.0.0
PyMuPDF>=1.23.0

# Celery + Redis
celery[redis]>=5.3.0
redis>=5.0.0

# Gunicorn (production server)
gunicorn>=22.0.0

# VnCoreNLP (tùy chọn - bỏ comment khi đã tải JAR + models)
# vncorenlp>=1.0.3
